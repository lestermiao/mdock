apiVersion: v1
kind: Service
metadata:
    name: "testcluster-9206"
    labels:
        app: "testcluster-9206"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "testcluster-9206"